import { addTokens, getUser, updateUser } from '../../database/index.js';
export default { name:'battle', aliases:['batalha','fight'], category:'rpg', description:'Batalha fictícia contra um inimigo', async execute({sender,reply}) { const enemy=['Goblin','Slime','Guardião','Lobo Sombrio'][Math.floor(Math.random()*4)]; const win=Math.random()<0.65; const u=getUser(sender); if(win){const reward=150+Math.floor(Math.random()*250); const xp=u.xp+40; addTokens(sender,reward); updateUser(sender,{xp,level:Math.floor(xp/500)+1}); return reply(`⚔️ *VITÓRIA!*\n\n👾 ${enemy} derrotado.\n🪙 +${reward} Tokens\n⭐ +40 XP`);} return reply(`💥 *DERROTA!*\n\n👾 ${enemy} venceu a batalha.\nTente novamente.`); } };
